import 'package:flutter/material.dart';

class Category {
  final int index;
  final String name;
  final Image icon;
  final Color color;

  Category(this.index, this.name, this.icon, this.color);
}
